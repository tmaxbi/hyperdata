apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: {{ .Release.Namespace }}-hd
  name: {{ .Release.Namespace }}-pvc-db
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 40Gi
  storageClassName: csi-cephfs
  volumeMode: Filesystem